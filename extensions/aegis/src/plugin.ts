import type { OpenClawPluginApi } from "openclaw/plugin-sdk/plugin-entry";

export function registerAegisPlugin(_api: OpenClawPluginApi): void {
  // Will wire up IPC client, audit emitter, daemon service, and hook handlers.
  throw new Error("Not implemented");
}
